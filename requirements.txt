# Core (Python 3.12)
numpy>=1.26
torch>=2.3
stable-baselines3>=2.3.0
gymnasium>=0.29
hydra-core>=1.3
omegaconf>=2.3
pandas>=2.2
scipy>=1.13
tqdm>=4.66

# VLA inference / VLM reward generation
transformers>=4.51
accelerate>=1.0
# vllm>=0.6            # для запуска Qwen2.5-VL-7B-Instruct (серия 5)
# qwen-vl-utils

# Benchmarks (устанавливаются отдельно по инструкциям проектов):
#   LIBERO + robosuite  -> https://github.com/Lifelong-Robot-Learning/LIBERO
#   MetaWorld           -> https://github.com/Farama-Foundation/Metaworld
#
# Веса VLA-моделей (HuggingFace / официальные репозитории):
#   OpenVLA-OFT  -> moojink/openvla-7b-oft-finetuned-libero-*
#   UniVLA, pi0 / pi0-fast (openpi), CogACT, SmolVLA, SpatialVLA,
#   Octo-Small, RT-1-X (симуляционный адаптер)
